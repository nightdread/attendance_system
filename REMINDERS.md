# 🔔 Система напоминаний об уходе

## Описание

Автоматическая система напоминаний помогает сотрудникам не забывать отмечать уход из офиса. Если сотрудник отметил приход, но забыл отметить уход, бот автоматически напомнит ему через 8 часов.

## Как это работает

1. **Планировщик задач** проверяет открытые сессии каждые 30 минут
2. **Находит сессии** старше 8 часов (последнее событие = "in")
3. **Отправляет напоминание** в Telegram с кнопкой "✅ Да, ушел"
4. **Автоматически закрывает сессию** при нажатии на кнопку

## Функции

### Автоматические напоминания

- ⏰ Проверка каждые **30 минут**
- 🕐 Напоминание через **8 часов** после прихода
- 🔄 Одно напоминание на сессию (избегает спама)
- 🧹 Автоматическая очистка отслеживания закрытых сессий

### Интерактивная кнопка

При получении напоминания пользователь видит:
```
⏰ Напоминание

Вы отметили приход в 09:00
Прошло уже 8 часов.

🏠 Не забыли отметить уход?

[✅ Да, ушел]
```

Нажатие на кнопку:
- ✅ Автоматически создает событие "out"
- 📝 Показывает подтверждение с временем
- 🧹 Удаляет напоминание из отслеживания

## Технические детали

### База данных

Новый метод `get_open_sessions_older_than(hours)`:
- Находит пользователей с открытыми сессиями
- Фильтрует по времени (старше N часов)
- Возвращает информацию о сессии и времени

### Планировщик (APScheduler)

- **AsyncIOScheduler** для асинхронных задач
- **IntervalTrigger** для периодических проверок
- Интеграция с python-telegram-bot

### Обработка ошибок

- Игнорирование заблокированных ботов
- Логирование всех операций
- Автоматическая очистка неактивных напоминаний

## Настройка

### Изменение интервала проверки

В `bot/bot.py`, функция `main()`:
```python
scheduler.add_job(
    bot.check_and_send_reminders,
    trigger=IntervalTrigger(minutes=30),  # Изменить здесь
    id='check_reminders',
    replace_existing=True
)
```

### Изменение времени напоминания

В `bot/bot.py`, функция `check_and_send_reminders()`:
```python
open_sessions = self.db.get_open_sessions_older_than(8.0)  # Изменить здесь (часы)
```

## Логирование

Все действия логируются:
- `INFO`: Отправка напоминания
- `ERROR`: Ошибки при отправке
- `DEBUG`: Очистка отслеживания

## Примеры использования

### Ручная проверка (для тестирования)

```python
from database import Database
db = Database('attendance.db')

# Найти сессии старше 6 часов
sessions = db.get_open_sessions_older_than(6.0)
for session in sessions:
    print(f"{session['fio']}: {session['hours_open']:.1f} часов")
```

## Будущие улучшения

- [ ] Настраиваемое время напоминания через конфигурацию
- [ ] Множественные напоминания (через 8, 10, 12 часов)
- [ ] Автоматическое закрытие сессии через 24 часа
- [ ] Статистика забытых уходов
- [ ] Уведомления администраторам о забытых уходах

