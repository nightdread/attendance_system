# Список улучшений и исправлений для проекта

## 🔴 Критические исправления (баги)

### 1. Неправильное использование `log_error()`
**Проблема:** Функция `log_error()` ожидает `(error: Exception, context: str = "")`, но вызывается со строкой.

**Файлы:**
- `backend/main.py:285` - `log_error(f"Login error: {e}")`
- `backend/main.py:394` - `log_error(f"Self dashboard error: {e}")`
- `backend/main.py:437` - `log_error(f"Analytics page token validation error: {e}")`
- `backend/main.py:470` - `log_error(f"User management page error: {e}")`

**Исправление:** Изменить вызовы на `log_error(e, "Login")` или изменить сигнатуру функции.

### 2. Потенциальная ошибка KeyError в rate limiting
**Проблема:** В `backend/main.py:237` обращение к `LOGIN_ATTEMPTS[client_ip]` может вызвать KeyError, если ключа нет.

**Исправление:** Использовать `LOGIN_ATTEMPTS.get(client_ip, [])` или `defaultdict`.

### 3. Дублирование вызова `authorize_request()`
**Проблема:** В `backend/main.py:495` и `498` функция `authorize_request()` вызывается дважды.

**Исправление:** Сохранить результат первого вызова в переменную.

### 4. Потенциальная ошибка при обращении к `current_user.get("id")`
**Проблема:** В `backend/main.py:501` если `current_user` равен `None`, вызов `.get("id")` вызовет AttributeError.

**Исправление:** Добавить проверку `if current_user and current_user.get("id")`.

## ⚠️ Важные улучшения

### 5. Улучшение обработки ошибок
**Проблема:** Множество мест с `except Exception: pass`, которые скрывают ошибки.

**Файлы:**
- `backend/main.py:247-249` - rate limiting
- `backend/main.py:262-263` - сброс счетчика попыток

**Улучшение:** Добавить логирование или более специфичную обработку ошибок.

### 6. Валидация входных данных
**Проблема:** Отсутствует валидация длины и формата для:
- Username при логине
- FIO в боте (только минимальная длина 3 символа)
- Пароли (нет проверки сложности)
- Токены и другие параметры

**Улучшение:** Добавить валидацию с использованием Pydantic моделей или вручную.

### 7. Защита от SQL-инъекций
**Проблема:** Хотя используется параметризованные запросы, стоит провести аудит всех SQL-запросов.

**Улучшение:** Проверить все места использования `cursor.execute()` в `database.py`.

### 8. Защита от XSS
**Проблема:** Нужно проверить шаблоны на экранирование пользовательского ввода.

**Улучшение:** Убедиться, что Jinja2 автоматически экранирует все переменные в шаблонах.

## 📋 Функциональные улучшения (из TODO)

### 9. ESP32 интеграция
**Статус:** Документация есть (`MICROCONTROLLER_INTEGRATION.md`), но интеграция не реализована.

**Требуется:** 
- Тестирование с реальным устройством
- Возможно, оптимизация endpoint `/api/token` для устройств с ограниченными ресурсами

### 10. PostgreSQL поддержка
**Статус:** Сейчас используется только SQLite.

**Требуется:**
- Абстракция слоя БД (SQLAlchemy или другой ORM)
- Миграции (Alembic)
- Конфигурация для выбора БД (SQLite/PostgreSQL)

### 11. Более детальная отчетность
**Статус:** Базовая аналитика есть, но можно расширить.

**Требуется:**
- Отчеты по периодам (месяц, квартал, год)
- Сравнение периодов
- Статистика по отделам/должностям
- Экспорт в различные форматы

### 12. Экспорт данных в Excel
**Статус:** Не реализовано.

**Требуется:**
- Endpoint для экспорта событий в Excel
- Форматирование таблиц
- Графики в Excel (опционально)

### 13. Мобильное приложение
**Статус:** Не реализовано.

**Требуется:** 
- Определить платформу (iOS/Android/React Native)
- API для мобильного приложения
- Push-уведомления

### 14. Push-уведомления
**Статус:** Не реализовано.

**Требуется:**
- Интеграция с Firebase/APNs
- Настройки уведомлений для пользователей
- Уведомления о событиях (приход/уход, напоминания)

## 🔒 Безопасность

### 15. Улучшение rate limiting
**Проблема:** Rate limiting работает только для логина, но не для других endpoints.

**Улучшение:** Добавить rate limiting для:
- API endpoints
- Создание токенов
- Регистрация пользователей

### 16. CSRF защита
**Проблема:** Нужно проверить наличие CSRF токенов для форм.

**Улучшение:** Добавить CSRF middleware для FastAPI.

### 17. Логирование безопасности
**Улучшение:** Расширить логирование:
- Неудачные попытки входа
- Подозрительная активность
- Изменения в правах доступа
- Экспорт данных

### 18. Ротация секретов
**Статус:** JWT ротация поддерживается, но нет автоматизации.

**Улучшение:** 
- Скрипт для ротации ключей
- Документация процесса
- Автоматические напоминания

## 🧪 Тестирование

### 19. Расширение тестового покрытия
**Требуется:**
- Тесты для rate limiting
- Тесты для обработки ошибок
- Интеграционные тесты для бота
- Тесты для edge cases (истекшие токены, невалидные данные)
- Тесты производительности

### 20. Тесты безопасности
**Требуется:**
- Тесты на SQL-инъекции
- Тесты на XSS
- Тесты на авторизацию
- Тесты на rate limiting

## 📊 Мониторинг и производительность

### 21. Метрики и мониторинг
**Требуется:**
- Prometheus метрики
- Health checks с детальной информацией
- Мониторинг производительности БД
- Алерты на критические ошибки

### 22. Оптимизация запросов к БД
**Требуется:**
- Индексы для часто используемых запросов
- Оптимизация аналитических запросов
- Connection pooling (для PostgreSQL)

### 23. Кэширование
**Статус:** Redis кэширование есть, но можно расширить.

**Улучшение:**
- Кэширование результатов аналитики
- Кэширование списка пользователей
- TTL для различных типов данных

## 📝 Документация

### 24. API документация
**Статус:** Swagger есть, но можно улучшить.

**Улучшение:**
- Примеры запросов/ответов
- Описание ошибок
- Rate limiting информация
- Версионирование API

### 25. Документация для разработчиков
**Требуется:**
- Архитектурная документация
- Описание процесса деплоя
- Руководство по добавлению новых функций
- Troubleshooting guide

## 🚀 DevOps

### 26. CI/CD
**Требуется:**
- GitHub Actions / GitLab CI
- Автоматические тесты
- Автоматический деплой
- Проверка безопасности (dependabot, snyk)

### 27. Бэкапы
**Требуется:**
- Автоматические бэкапы БД
- Хранение бэкапов (S3, etc.)
- Процедура восстановления
- Тестирование восстановления

### 28. Логирование в продакшене
**Улучшение:**
- Централизованное логирование (ELK, Loki)
- Структурированные логи
- Retention политики
- Алерты на ошибки

## 🎨 UX/UI улучшения

### 29. Улучшение интерфейса
**Требуется:**
- Адаптивный дизайн для мобильных
- Темная тема
- Улучшенная навигация
- Индикаторы загрузки

### 30. Локализация
**Статус:** Частично на русском, частично на английском.

**Улучшение:** Полная локализация (i18n).

## 🔧 Технический долг

### 31. Рефакторинг кода
**Требуется:**
- Разделение бизнес-логики и представления
- Улучшение структуры проекта
- Удаление дублирующегося кода
- Типизация (type hints везде)

### 32. Обновление зависимостей
**Требуется:**
- Регулярное обновление зависимостей
- Проверка на уязвимости
- Обновление Python (если нужно)

## Приоритеты

### Высокий приоритет (сделать в первую очередь):
1. Исправление багов с `log_error()` (#1)
2. Исправление KeyError в rate limiting (#2)
3. Дублирование `authorize_request()` (#3)
4. Улучшение обработки ошибок (#5)
5. Валидация входных данных (#6)

### Средний приоритет:
6. Расширение тестов (#19)
7. Улучшение безопасности (#15, #16)
8. Оптимизация БД (#22)
9. Бэкапы (#27)

### Низкий приоритет (будущие улучшения):
10. PostgreSQL поддержка (#10)
11. Мобильное приложение (#13)
12. Push-уведомления (#14)
13. Экспорт в Excel (#12)

