# Система учета прихода/ухода сотрудников

Система учета рабочего времени на базе Telegram-бота и QR-кодов.

**Текущая версия:** 1.2.0

**Последние обновления:**
- ✅ Экспорт отчетов в Excel (pivot table формат)
- ✅ Отправка отчетов по email
- ✅ Экспорт событий в iCal формат
- ✅ Интерактивный календарь с выделением выходных дней
- ✅ PWA (Progressive Web App) поддержка
- ✅ Адаптивный дизайн для мобильных устройств
- ✅ IP whitelist для администраторов
- ✅ Интеграция с производственным календарем
- ✅ Расширенная аналитика (сравнение периодов, опоздания, переработки)
- ✅ Учет отпусков и больничных
- ✅ Шаблоны отчетов
- ✅ Аудит логи
- ✅ Автоматические напоминания от бота (с учетом выходных дней)
- ✅ Валидация входных данных
- ✅ Rate limiting для всех API endpoints
- ✅ CSRF защита
- ✅ Расширенное логирование безопасности
- ✅ Оптимизация БД (индексы, оптимизированные запросы)
- ✅ Автоматические бэкапы БД
- ✅ Ротация секретов (JWT ключи)
- ✅ Метрики и мониторинг
- ✅ Улучшенная API документация

## Возможности

### Основные функции
- ✅ **HTTPS проксирование** - Angie веб-сервер с SSL сертификатами
- ✅ **JWT Аутентификация** - безопасная аутентификация пользователей
- ✅ **Redis Кэширование** - высокая производительность (опционально)
- ✅ **Унифицированная система** - общий учет для всех сотрудников
- ✅ **Одноразовые QR-коды** - каждый код используется только один раз
- ✅ **Автоматическая генерация** новых токенов после использования
- ✅ **Регистрация пользователей** через Telegram-бот
- ✅ **Отметка прихода/ухода** для всех сотрудников
- ✅ **Веб-терминал** для отображения QR-кодов
- ✅ **Интерактивная аналитика** - графики и отчеты в реальном времени
- ✅ **Экспорт отчетов в Excel** - pivot table формат с фамилиями, днями и часами
- ✅ **Отправка отчетов по email** - автоматическая отправка отчетов на email
- ✅ **Экспорт в iCal** - экспорт событий посещаемости в календарные приложения
- ✅ **Интерактивный календарь** - визуализация посещаемости с выделением выходных дней
- ✅ **PWA поддержка** - установка как мобильное приложение
- ✅ **Адаптивный дизайн** - оптимизация для мобильных устройств и планшетов
- ✅ **Структурированное логирование** с ротацией
- ✅ **API Документация** - Swagger/OpenAPI с примерами
- ✅ **Docker поддержка** - легкое развертывание

### Безопасность
- ✅ **Валидация входных данных** - проверка всех пользовательских данных
- ✅ **Rate Limiting** - защита от DDoS и злоупотреблений
- ✅ **CSRF защита** - защита от межсайтовых запросов
- ✅ **IP Whitelist** - ограничение доступа администраторов по IP адресам
- ✅ **Расширенное логирование безопасности** - аудит всех критических событий
- ✅ **Аудит логи** - детальное логирование всех операций
- ✅ **Ротация секретов** - безопасная смена JWT ключей без простоя

### Производительность и надежность
- ✅ **Оптимизация БД** - индексы и оптимизированные запросы
- ✅ **Автоматические бэкапы** - регулярное резервное копирование БД
- ✅ **Метрики и мониторинг** - детальные health checks и метрики производительности

### Расширенная аналитика и отчетность
- ✅ **Pivot таблицы** - отчеты с фамилиями сотрудников, днями и часами работы
- ✅ **Сравнение периодов** - сравнение статистики между разными периодами
- ✅ **Анализ опозданий** - выявление и статистика опозданий сотрудников
- ✅ **Учет переработок** - отслеживание сверхурочных часов
- ✅ **Недельное распределение** - анализ распределения рабочего времени по дням недели
- ✅ **Календарь посещаемости** - визуализация посещаемости по месяцам с выделением выходных
- ✅ **Топ работников** - рейтинг сотрудников по количеству отработанных часов
- ✅ **Статистика по отделам** - группировка данных по отделам

### HR функции
- ✅ **Учет отпусков** - регистрация и отслеживание отпусков сотрудников
- ✅ **Учет больничных** - регистрация и отслеживание больничных листов
- ✅ **Шаблоны отчетов** - сохранение и переиспользование настроек отчетов
- ✅ **Производственный календарь** - интеграция с производственным календарем для учета праздников

## Архитектура

- **Backend**: FastAPI + SQLite + Redis (опционально)
- **Bot**: Python Telegram Bot с APScheduler для автоматических задач
- **Web Interface**: HTML/JS с Chart.js для графиков, PWA поддержка
- **Web Server**: Angie (nginx fork) с HTTPS проксированием
- **Cache**: Redis или in-memory fallback
- **Database**: SQLite (файл `attendance.db`)
- **Authentication**: JWT tokens + Session-based auth
- **SSL**: Let's Encrypt сертификаты
- **Email**: SMTP для отправки отчетов
- **Export**: Excel (openpyxl), iCal (icalendar)
- **Timezone**: Настраиваемый часовой пояс (по умолчанию Europe/Moscow)

## 🚀 Быстрый старт

### Docker (рекомендуется)
```bash
git clone <repository-url>
cd attendance_system
cp .env.example .env
# Отредактируйте .env файл с вашими настройками
# Установите DOMAIN=your-domain.com для HTTPS
docker compose up -d
```

Приложение: `https://your-domain.com` (production)

## 📋 Настройка

### 1. Переменные окружения
Скопируйте пример файла и заполните своими значениями:
```bash
cp .env.example .env
```

Обязательные переменные:
- `SECRET_KEY` - секретный ключ для шифрования
- `BOT_TOKEN` - токен Telegram бота от @BotFather
- `BOT_USERNAME` - username бота
- `WEB_PASSWORD` - пароль для веб-админки
- `DOMAIN` - домен для HTTPS (например, your-domain.com)

Опциональные переменные:
- `ADMIN_IP_WHITELIST` - список разрешенных IP адресов для администраторов (через запятую)
- `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD` - настройки SMTP для отправки email
- `SMTP_FROM_EMAIL` - email адрес отправителя
- `TIMEZONE` - часовой пояс системы (по умолчанию Europe/Moscow)
- `PRODUCTION_CALENDAR_API_URL` - URL API производственного календаря (по умолчанию https://isdayoff.ru)
- `PRODUCTION_CALENDAR_FILE` - путь к локальному файлу производственного календаря (JSON)

### 2. Создание Telegram бота
1. Напишите [@BotFather](https://t.me/botfather)
2. Создайте нового бота командой `/newbot`
3. Скопируйте токен в `.env` как `BOT_TOKEN`
4. Скопируйте username бота в `.env` как `BOT_USERNAME`

### 3. Генерация секретных ключей
Используйте скрипт генерации ключей:
```bash
./generate-keys.sh
```

Или сгенерируйте вручную:
```bash
# SECRET_KEY (64 символа)
python3 -c "import secrets; print('SECRET_KEY=' + secrets.token_urlsafe(64))"

# API_KEY (32 символа) - опционально
python3 -c "import secrets; print('API_KEY=' + secrets.token_urlsafe(32))"

# JWT_SECRET_KEY (64 символа)
python3 -c "import secrets; print('JWT_SECRET_KEY=' + secrets.token_urlsafe(64))"
```

### 4. SSL сертификаты (для production)
Для работы HTTPS Angie автоматически использует сертификаты из директории `ssl/`:
- `ssl/fullchain.pem` - цепочка сертификатов
- `ssl/privkey.pem` - приватный ключ

Получите сертификаты Let's Encrypt:
```bash
# Используйте certbot или другой ACME клиент
# Скопируйте сертификаты в ssl/ директорию
```

### 5. Настройка отправки email (опционально)
Для отправки отчетов по email настройте SMTP в `.env`:
```bash
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=your-email@gmail.com
```

**Примечание:** Для Gmail используйте "Пароль приложения" вместо обычного пароля.

### 6. Настройка часового пояса
Система использует часовой пояс для отображения времени. По умолчанию используется `Europe/Moscow` (GMT+3).

Для изменения часового пояса установите переменную `TIMEZONE` в `.env`:
```bash
TIMEZONE=Europe/Moscow  # или UTC, America/New_York и т.д.
```

**Важно:** Данные в базе хранятся в UTC. Часовой пояс влияет только на отображение времени в интерфейсе.

### 7. Настройка производственного календаря (опционально)
Система может использовать производственный календарь для определения выходных и праздничных дней.

**Вариант 1: Использование API (по умолчанию)**
```bash
PRODUCTION_CALENDAR_API_URL=https://isdayoff.ru
```

**Вариант 2: Локальный файл**
Создайте JSON файл с календарем и укажите путь:
```bash
PRODUCTION_CALENDAR_FILE=/app/data/production_calendar.json
```

Формат файла:
```json
{
  "2024": {
    "01-01": "holiday",
    "01-07": "holiday",
    ...
  }
}
```

Производственный календарь используется для:
- Выделения выходных дней в календаре аналитики
- Пропуска автоматических напоминаний от бота в выходные дни

## 🐳 Запуск через Docker

### Production (с SSL)
```bash
# Установите домен в .env: DOMAIN=your-domain.com
# Поместите SSL сертификаты в ssl/ директорию
docker compose up -d
```

### Сборка образов
```bash
# Сборка и запуск
docker compose up -d

# Или только сборка
docker compose build
```

## Использование

### 1. Настройка бота
- Создайте бота через [@BotFather](https://t.me/botfather)
- Получите токен и username бота
- Установите их в `.env` файл

### 2. Доступ к веб-терминалу
- Откройте браузер: `https://your-domain.com/login`
- Введите логин/пароль из `.env` файла
- Выберите локацию и сканируйте QR-код

### 3. Работа с ботом
- Пользователи сканируют QR-код на терминале
- При первом использовании вводят ФИО
- Выбирают "Пришёл" или "Ушёл"

### 4. Команды бота
- `/start` - начало работы
- `/my_last` - последние события пользователя
- `/who_here` - кто сейчас в офисе

### 5. Автоматические уведомления бота
- Напоминания о необходимости отметиться (только в рабочие дни)
- Еженедельные сводки по посещаемости
- Напоминания в конце месяца о необходимости проверить отчеты

### 6. Админ-панель и аналитика
- `https://your-domain.com/admin` - просмотр присутствующих
- `/admin/user/{id}` - история пользователя
- `/analytics` - расширенная аналитика с графиками, календарем и экспортом отчетов
- `/users` - управление пользователями, отделами и ролями

### 7. Экспорт отчетов
В разделе аналитики доступны следующие варианты экспорта:
- **Excel (pivot table)** - отчет с фамилиями сотрудников, днями и часами работы
  - Поддерживаемые периоды: "последняя неделя", "последний месяц", "текущий месяц", произвольный диапазон дат
  - Формат времени: часы:минуты (например, 8:30)
- **Email** - отправка отчета на указанный email адрес
- **iCal** - экспорт событий посещаемости для календарных приложений

## API Endpoints

### Основные endpoints

**Токены:**
- `GET /api/active_token` - получить активный токен (требует аутентификацию, rate limit: 10/мин)
- `GET /api/token` - получить токен для микроконтроллеров (опциональный API key)

**Пользователи:**
- `GET /api/user/{user_id}` - получить пользователя (требует admin/manager, rate limit: 20/мин)
- `PUT /api/user/{user_id}` - обновить пользователя (требует admin/manager, CSRF токен, rate limit: 10/мин)

**Аналитика:**
- `GET /api/analytics/daily/{date}` - дневная статистика (требует admin/manager/hr, rate limit: 30/мин)
- `GET /api/analytics/weekly` - недельная статистика (требует admin/manager/hr, rate limit: 30/мин)
- `GET /api/analytics/hourly/{date}` - почасовая статистика (требует admin/manager/hr, rate limit: 30/мин)
- `GET /api/analytics/compare` - сравнение периодов (требует admin/manager/hr, rate limit: 30/мин)
- `GET /api/analytics/late-arrivals` - анализ опозданий (требует admin/manager/hr, rate limit: 30/мин)
- `GET /api/analytics/overtime` - учет переработок (требует admin/manager/hr, rate limit: 30/мин)
- `GET /api/analytics/weekly-distribution` - недельное распределение (требует admin/manager/hr, rate limit: 30/мин)
- `GET /api/analytics/calendar/{year}/{month}` - данные календаря за месяц (требует admin/manager/hr, rate limit: 30/мин)
- `GET /api/employees/date/{date}` - список сотрудников за дату (требует admin/manager/hr, rate limit: 30/мин)

**Экспорт данных:**
- `GET /api/export/pivot` - экспорт pivot отчета в Excel (требует admin/manager/hr, rate limit: 10/мин)
- `POST /api/export/send-email` - отправить отчет по email (требует admin/manager/hr, rate limit: 5/мин)
- `GET /api/export/ical` - экспорт событий в iCal формат (требует admin/manager/hr, rate limit: 10/мин)

**HR функции:**
- `GET /api/vacations` - список отпусков (требует admin/manager/hr, rate limit: 30/мин)
- `POST /api/vacations` - создать отпуск (требует admin/manager/hr, rate limit: 10/мин)
- `GET /api/sick-leaves` - список больничных (требует admin/manager/hr, rate limit: 30/мин)
- `POST /api/sick-leaves` - создать больничный (требует admin/manager/hr, rate limit: 10/мин)
- `GET /api/report-templates` - список шаблонов отчетов (требует admin/manager/hr, rate limit: 30/мин)
- `POST /api/report-templates` - создать шаблон отчета (требует admin/manager/hr, rate limit: 10/мин)
- `DELETE /api/report-templates/{template_id}` - удалить шаблон (требует admin/manager/hr, rate limit: 10/мин)

**Аудит:**
- `GET /api/audit-log` - просмотр логов аудита (требует admin, rate limit: 30/мин)

**Мониторинг:**
- `GET /api/health` - проверка здоровья системы
- `GET /api/metrics` - детальные метрики производительности

**Полная документация:** `http://localhost:8000/docs`

### Веб-интерфейсы
- `/login` — вход в систему
- `/terminal` — публичный терминал с QR-кодом (сессия помечается для вызова `/api/active_token`)
- `/admin` — админ-панель (требует логин)
- `/analytics` — расширенная аналитика с графиками и календарем (требует admin/manager/hr)
- `/users` — управление пользователями (требует admin/manager)
- `/me` — личный кабинет сотрудника
- `/logout` — выход
- Все URLs доступны по HTTPS в production
- PWA поддержка: установка как мобильное приложение через браузер

## Хранение БД вне контейнера
- Переменная `DB_PATH` задаёт путь к файлу SQLite внутри контейнера (по умолчанию `/app/attendance.db`).
- Чтобы хранить БД на хосте, примонтируйте файл/директорию и задайте `DB_PATH`:
  ```yaml
  # docker-compose.yml (пример)
  services:
    attendance_app:
      environment:
        - DB_PATH=/data/attendance.db
      volumes:
        - ./data:/data        # каталог на хосте
    attendance_bot:
      environment:
        - DB_PATH=/data/attendance.db
      volumes:
        - ./data:/data
  ```
- Если БД уже существует на хосте, укажите на неё путь в `DB_PATH` и примонтируйте соответствующий файл/каталог.
- Для внешних СУБД (PostgreSQL/MySQL) требуется доработка кода (сейчас прямое использование `sqlite3`).

## Секреты и ротация

### Ротация JWT ключей
Система поддерживает zero-downtime ротацию JWT секретных ключей:

```bash
# Проверить текущий статус
python3 tools/rotate_jwt_keys.py --status

# Выполнить ротацию
python3 tools/rotate_jwt_keys.py

# После ротации перезапустить контейнеры
docker compose restart attendance_app attendance_bot
```

**Как это работает:**
- `JWT_SECRET_KEY` - текущий ключ для подписи новых токенов
- `JWT_SECRET_KEY_PREV` - предыдущий ключ для верификации старых токенов
- Старые токены продолжают работать до истечения срока (обычно 30 минут)

Подробнее: см. `JWT_ROTATION_GUIDE.md`

### Другие секреты
- Сессии: `SESSION_SECRET_KEY` (по умолчанию `SECRET_KEY`). При смене ключа старые сессии станут невалидны — планируйте окна деплоя.

### Дефолтные пользователи (создаются автоматически на пустой БД)
- admin (role=admin)
- manager (role=manager)
- hr (role=hr)

Пароли генерируются при первой инициализации пустой БД и выводятся в stdout (логи контейнера). Внутри контейнера не сохраняются.

## Структура базы данных

### Таблицы:
- `people` - пользователи (tg_user_id, fio, username)
- `events` - события (user_id, location, action, timestamp)
- `tokens` - токены (token, location, used, timestamps)
- `web_users` - веб-пользователи (username, password_hash, role, department, position)

### Оптимизация
База данных оптимизирована с помощью индексов:
- Индексы по времени событий для быстрой аналитики
- Индексы по действиям (in/out) для фильтрации
- Индексы по ролям и статусу пользователей
- Составные индексы для сложных запросов

Все аналитические запросы оптимизированы для использования индексов.

## Разработка

### Добавление новой локации
Добавьте локацию в `config/config.py` в список `LOCATIONS`.

### Кастомизация
- Шаблоны: `templates/`
- Стили: редактируйте HTML в шаблонах
- Логика: `backend/database.py`, `bot/bot.py`

## Безопасность

### Защита данных
- **HTTPS шифрование** - все соединения защищены SSL/TLS
- **Валидация входных данных** - строгая проверка всех пользовательских вводов
- **CSRF защита** - все модифицирующие операции защищены CSRF токенами
- **SQL Injection защита** - параметризованные запросы ко всем БД операциям
- **XSS защита** - автоматическое экранирование в шаблонах Jinja2

### Аутентификация и авторизация
- Обязательные секреты из ENV: `SECRET_KEY`, `BOT_TOKEN`, `BOT_USERNAME`, `WEB_PASSWORD`; при необходимости `API_KEY`.
- JWT токены с поддержкой ротации ключей (zero-downtime)
- Роли пользователей: admin, manager, hr, user, terminal
- `/api/analytics/*` и `/api/active_token` требуют API-ключ или авторизованную сессию.

### Защита от атак
- **Rate Limiting** - ограничение частоты запросов:
  - `/api/active_token`: 10 запросов/минуту
  - `/api/user/*`: 20 запросов/минуту (GET), 10 запросов/минуту (PUT)
  - `/api/analytics/*`: 30 запросов/минуту
  - `/api/export/*`: 10 запросов/минуту (GET), 5 запросов/минуту (POST)
  - `/login`: 5 попыток/5 минут (блокировка на 15 минут)
- **IP Whitelist** - ограничение доступа администраторов по IP адресам (опционально)
- QR-коды одноразовые, токены истекают через 24 часа
- Логирование всех попыток входа и подозрительной активности

### Мониторинг безопасности
- Расширенное логирование безопасности (неудачные логины, изменения прав, подозрительная активность)
- Аудит всех критических операций
- SSL сертификаты Let's Encrypt с автоматическим обновлением

### Рекомендации
- Регулярные бэкапы БД (см. раздел "Бэкапы")
- Ротация секретов каждые 90 дней (см. раздел "Ротация секретов")
- Мониторинг логов на подозрительную активность
- Данные хранятся локально в SQLite (рассмотрите бэкапы/шифрование/перенос в внешний DB)

## Бэкапы

Система поддерживает автоматические бэкапы базы данных:

```bash
# Создать бэкап вручную
python3 tools/backup_db.py --verify

# Установить автоматические бэкапы (cron)
./tools/setup_backup_cron.sh

# Восстановить из бэкапа
python3 tools/restore_db.py backups/attendance_backup_YYYYMMDD_HHMMSS.db.gz
```

**Возможности:**
- Автоматические ежедневные бэкапы (через cron)
- Сжатие бэкапов (gzip)
- Автоматическая очистка старых бэкапов
- Проверка целостности бэкапов
- Опциональная загрузка в S3

Подробнее: см. `BACKUP_README.md`

## Мониторинг и метрики

### Health Checks
```bash
# Базовый health check
curl http://localhost:8000/api/health

# Детальные метрики
curl http://localhost:8000/api/metrics
```

**Метрики включают:**
- Статус базы данных (подключение, размер)
- Статус Redis (подключение, использование памяти)
- Системные метрики (CPU, память, диск) - если установлен psutil
- Статистика приложения (пользователи, события, токены)

### API Документация
- **Swagger UI**: `http://localhost:8000/docs`
- **ReDoc**: `http://localhost:8000/redoc`
- **OpenAPI JSON**: `http://localhost:8000/openapi.json`

Документация включает:
- Примеры запросов/ответов
- Описание всех ошибок
- Информацию о rate limiting
- Требования к аутентификации

## TODO (будущие улучшения)

### ✅ Выполнено

- [x] **HTTPS проксирование (Angie)** - настроен Angie веб-сервер с SSL
- [x] **SSL сертификаты Let's Encrypt** - поддержка SSL/TLS сертификатов
- [x] **Валидация входных данных** - полная валидация всех пользовательских вводов (username, password, FIO, role, department, position)
- [x] **Rate limiting** - защита всех API endpoints от злоупотреблений
- [x] **CSRF защита** - защита всех модифицирующих операций CSRF токенами
- [x] **IP Whitelist** - ограничение доступа администраторов по IP адресам
- [x] **Расширенное логирование безопасности** - логирование всех критических событий (логины, изменения прав, подозрительная активность)
- [x] **Аудит логи** - детальное логирование всех операций в системе
- [x] **Оптимизация БД (индексы)** - добавлены индексы для всех часто используемых запросов, оптимизированы аналитические запросы
- [x] **Автоматические бэкапы** - система автоматических бэкапов БД с поддержкой cron и S3
- [x] **Ротация секретов** - zero-downtime ротация JWT ключей
- [x] **Метрики и мониторинг** - детальные health checks и метрики производительности
- [x] **Улучшение API документации** - полная Swagger документация с примерами, описанием ошибок и rate limiting
- [x] **Экспорт данных в Excel** - экспорт pivot отчетов в Excel формат с форматированием времени (часы:минуты)
- [x] **Отправка отчетов по email** - автоматическая отправка отчетов на email адреса
- [x] **Экспорт в iCal** - экспорт событий посещаемости в календарные приложения
- [x] **Расширенная отчетность** - расширенные отчеты по периодам, сравнение периодов, статистика по отделам
- [x] **Интерактивный календарь** - визуализация посещаемости с выделением выходных и праздничных дней
- [x] **PWA поддержка** - установка как мобильное приложение через браузер
- [x] **Адаптивный дизайн** - оптимизация интерфейса для мобильных устройств и планшетов
- [x] **Производственный календарь** - интеграция с производственным календарем для учета праздников
- [x] **Учет отпусков и больничных** - регистрация и отслеживание отпусков и больничных листов
- [x] **Шаблоны отчетов** - сохранение и переиспользование настроек отчетов
- [x] **Автоматические уведомления бота** - напоминания о необходимости отметиться (с учетом выходных дней)
- [x] **Расширенная аналитика** - анализ опозданий, переработок, недельное распределение рабочего времени

### 🔄 В планах

- [ ] **ESP32 интеграция** - полная интеграция с микроконтроллерами (базовая поддержка через `/api/token` уже есть, требуется тестирование и документация)
- [ ] **PostgreSQL поддержка** - миграция с SQLite на PostgreSQL для продакшена (требуется абстракция слоя БД, миграции Alembic)
- [ ] **Мобильное приложение** - нативное мобильное приложение (iOS/Android) с полным функционалом
- [ ] **Push-уведомления** - интеграция с Firebase/APNs для push-уведомлений о событиях и напоминаниях
- [ ] **Интеграции** - интеграция с внешними системами учета рабочего времени и HR системами

## Лицензия

MIT License
