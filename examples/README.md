# Примеры кода для ESP32 с дисплеями

Этот каталог содержит готовые примеры кода для отображения QR-кодов на различных типах дисплеев, подключенных к ESP32.

## Доступные примеры

### 1. ESP32-4848S040C (esp32_4848s040c_display.ino)
**Специально для ESP32-4.0" Capacitive Touch (480x480)** - модель ESP32-4848S040C_I.

**Особенности:**
- Оптимизирован для разрешения 480x480 пикселей
- Поддержка сенсорного экрана (capacitive touch)
- Большой размер QR-кода для лучшей читаемости
- Управление подсветкой

**Библиотеки:**
- TFT_eSPI: https://github.com/Bodmer/TFT_eSPI
- QRCode: https://github.com/ricmoo/QRCode
- ArduinoJson: через Library Manager

**📖 Подробная инструкция:** См. файл `ESP32_4848S040C_SETUP.md` для детального руководства по подключению и настройке.

### 2. TFT дисплей (esp32_tft_display.ino)
Для цветных TFT дисплеев (например, ILI9341, ST7735, ST7789).

**Библиотеки:**
- TFT_eSPI: https://github.com/Bodmer/TFT_eSPI
- QRCode: https://github.com/ricmoo/QRCode
- ArduinoJson: через Library Manager

**Особенности:**
- Быстрое обновление (1 секунда)
- Цветной дисплей
- Хорошая читаемость QR-кода

### 2. E-Paper дисплей (esp32_epaper_display.ino)
Для электронных чернил (например, Waveshare 2.9", 4.2").

**Библиотеки:**
- GxEPD2: https://github.com/ZinggJM/GxEPD2
- QRCode: https://github.com/ricmoo/QRCode
- ArduinoJson: через Library Manager

**Особенности:**
- Энергосбережение (обновление только при изменении)
- Отличная читаемость при любом освещении
- Медленное обновление (2 секунды)

### 3. OLED дисплей (esp32_oled_display.ino)
Для OLED дисплеев (SSD1306 128x64, 128x32).

**Библиотеки:**
- Adafruit_SSD1306: https://github.com/adafruit/Adafruit_SSD1306
- Adafruit_GFX: https://github.com/adafruit/Adafruit-GFX-Library
- QRCode: https://github.com/ricmoo/QRCode
- ArduinoJson: через Library Manager

**Особенности:**
- Компактный размер
- Низкое энергопотребление
- Быстрое обновление (1 секунда)

## Настройка

### 1. Установка библиотек

В Arduino IDE:
1. Откройте **Sketch → Include Library → Manage Libraries**
2. Установите следующие библиотеки:
   - **ArduinoJson** (автор: Benoit Blanchon)
   - **QRCode** (автор: ricmoo)
   - Для TFT: **TFT_eSPI** (автор: Bodmer)
   - Для e-Paper: **GxEPD2** (автор: Jean-Marc Zingg)
   - Для OLED: **Adafruit SSD1306** и **Adafruit GFX Library**

### 2. Настройка параметров

Откройте нужный файл `.ino` и измените следующие параметры:

```cpp
// Настройки WiFi
const char* ssid = "YOUR_WIFI_SSID";
const char* password = "YOUR_WIFI_PASSWORD";

// Настройки сервера
const char* serverUrl = "http://your-server:8000/api/token";
const char* apiKey = "";  // Опционально, если настроен API_KEY на сервере
```

**Важно:**
- Замените `your-server` на IP-адрес или домен вашего сервера
- Если на сервере настроен `API_KEY`, укажите его в переменной `apiKey`
- Убедитесь, что ESP32 может подключиться к серверу (проверьте firewall)

### 3. Настройка дисплея

#### TFT дисплей
- Отредактируйте файл `User_Setup.h` в библиотеке TFT_eSPI для настройки пинов
- Или измените пины в коде, если используете другой способ подключения

#### E-Paper дисплей
- Выберите правильную модель дисплея в начале файла (раскомментируйте нужную строку)
- Настройте пины подключения в конструкторе `display()`

#### OLED дисплей
- Для I2C: используйте стандартные пины (SDA=21, SCL=22)
- Для SPI: раскомментируйте секцию SPI и настройте пины

### 4. Загрузка кода

1. Подключите ESP32 к компьютеру через USB
2. В Arduino IDE выберите:
   - **Tools → Board → ESP32 Arduino → ESP32 Dev Module** (или вашу модель)
   - **Tools → Port → COMx** (выберите порт вашего ESP32)
3. Нажмите **Upload**

## Проверка работы

### 1. Проверка подключения к WiFi
После загрузки кода откройте Serial Monitor (115200 baud) и проверьте:
- Подключение к WiFi
- IP-адрес ESP32
- Успешные HTTP запросы к серверу

### 2. Проверка API сервера
Убедитесь, что сервер доступен:
```bash
curl http://your-server:8000/api/token
```

Должен вернуться JSON с токеном:
```json
{
  "token": "abc123xyz",
  "url": "https://t.me/your_bot?start=abc123xyz",
  "bot_username": "your_bot",
  "created_at": "2025-01-25T10:30:45.123456"
}
```

### 3. Проверка отображения QR-кода
- На дисплее должен отображаться QR-код
- QR-код обновляется автоматически при изменении токена
- Попробуйте отсканировать QR-код через Telegram

## Устранение проблем

### ESP32 не подключается к WiFi
- Проверьте SSID и пароль
- Убедитесь, что WiFi работает на частоте 2.4 GHz (ESP32 не поддерживает 5 GHz)
- Проверьте расстояние до роутера

### HTTP ошибки
- Проверьте доступность сервера: `ping your-server`
- Проверьте firewall и порты
- Убедитесь, что сервер работает: `curl http://your-server:8000/api/health`

### QR-код не отображается
- Проверьте подключение дисплея
- Убедитесь, что установлены все необходимые библиотеки
- Проверьте настройки пинов в коде
- Откройте Serial Monitor для диагностики

### QR-код не сканируется
- Убедитесь, что QR-код достаточно большой на экране
- Проверьте освещение (особенно для OLED)
- Попробуйте увеличить масштаб QR-кода в коде (переменная `scale`)

### Дисплей показывает ошибки
- Проверьте логи в Serial Monitor
- Убедитесь, что все библиотеки установлены правильно
- Проверьте версии библиотек (могут быть несовместимы)

## Оптимизация

### Энергосбережение
Для e-Paper дисплеев можно увеличить интервал проверки:
```cpp
const unsigned long UPDATE_INTERVAL = 5000;  // 5 секунд вместо 2
```

### Производительность
Для быстрого обновления на TFT/OLED:
```cpp
const unsigned long UPDATE_INTERVAL = 500;  // 0.5 секунды
```

### Размер QR-кода
Для маленьких дисплеев используйте меньшую версию QR-кода:
```cpp
uint8_t qrcodeData[qrcode_getBufferSize(2)];  // Версия 2 вместо 3
qrcode_initText(&qrcode, qrcodeData, 2, 0, url.c_str());
```

## Дополнительные возможности

### Сохранение токена в EEPROM
Можно сохранять последний токен в EEPROM для восстановления после перезагрузки:
```cpp
#include <EEPROM.h>

void saveToken(String token) {
  EEPROM.begin(512);
  // Сохранить токен
  EEPROM.commit();
}
```

### Отображение дополнительной информации
Можно добавить отображение времени, статуса подключения и т.д.:
```cpp
display.setCursor(0, 0);
display.println("Token: " + token.substring(0, 8));
display.println("Time: " + String(millis() / 1000) + "s");
```

## Поддержка

При возникновении проблем:
1. Проверьте логи в Serial Monitor
2. Убедитесь, что все библиотеки установлены
3. Проверьте документацию библиотек
4. Проверьте документацию проекта: `MICROCONTROLLER_INTEGRATION.md`
